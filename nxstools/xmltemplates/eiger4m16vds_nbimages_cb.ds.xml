<?xml version='1.0'?>
<definition>
  <datasource type="PYEVAL" name="$(name)_nbimages_cb">
    <result name="result">
try:
    import tango
except Exception:
    import PyTango as tango
ds.result = tango.DeviceProxy("$(hostname)/$(device)").NbImages
    </result>
  </datasource>
</definition>
