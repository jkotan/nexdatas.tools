<?xml version='1.0'?>
<definition>
  <datasource type="PYEVAL" name="$(name)">
    <result name="result">
from nxstools.pyeval import secop
from nxswriter import globallock
ds.result = secop.secop_group_cmd("$(message)", "$(host)", $(port),  $(timeout),  "$(group)",  $(access), globallock, commonblock)
    </result>
  </datasource>
</definition>
